// Array de inteiros.

let meusNumeros = [1, 3, 7];

// Array de Strings.

let minhaCores = ["Azul", "Preto", "Vermelho"];

// Arrays de objestos.

let pets = [{nome: "Peludo", especie: "cachorro"}, {nome: "Louro-José"}, {especie: "Papagaio"}];

// Adicionando um valor na array.

meusNumeros.push(8);

// Imprimindo a array.

console.log(meusNumeros);

// Acessando uma array pelo index.

console.log(minhaCores[2]);

// Acessando uma array pelo index e pela chave.

console.log(pets[0].especie);
console.log(pets[1].nome);